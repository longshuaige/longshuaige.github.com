# 响应式数据流程
## init
* this._init =>{
    ...
    initState[state.js] =>{
        initProps,
        initMethods,
        initData => observe[observer/index.js]=>{
            new Observer(...)
        }
    }
}